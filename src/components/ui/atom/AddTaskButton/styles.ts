import styled from "styled-components";

export const Button = styled.button`
  padding: 16px;
  text-align: center;
  background: var(--blue-dark);
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
  border-radius: 8px;
  color: var(--gray-100);
  font-weight: 700;
`;
