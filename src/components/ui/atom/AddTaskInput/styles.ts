import styled from "styled-components";

export const Input = styled.input`
  width: calc(46rem - 90px);
  height: 3.5rem;
  padding: 16px;
  background: var(--gray-500);
  border: none;
  border-radius: 8px;
  color: var(--gray-300);
`;
